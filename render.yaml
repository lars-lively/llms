services:
  - type: cron
    name: llms-updater
    env: python
    schedule: "0 3 * * 1"  # Every Monday at 3 AM UTC
    buildCommand: pip install -r requirements.txt
    startCommand: python update_llms_txt.py
    plan: free 